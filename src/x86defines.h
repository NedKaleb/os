#ifndef X86DEFINES_H
#define X86DEFINES_H

// If set, interrupt will be output to the VGA buffer
#define OUTPUT_IRQS_TO_VGA
// If set, "100 ticks" will be output to the VGA buffer every 100 timer ticks
//#define OUTPUT_PIT_TICKS_TO_VGA

#endif
